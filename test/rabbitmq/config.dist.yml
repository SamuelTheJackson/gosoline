env: test

app_project: gosoline
app_family: test
app_name: rabbitmq-test

test:
  components:
    rabbitmq:
      default:
        use_external_container: true
        host: 127.0.0.1
        port: 5672

rabbitmq:
  default:
    endpoint: amqp://localhost:5672
    timeout: 20
    retry_count: 4

