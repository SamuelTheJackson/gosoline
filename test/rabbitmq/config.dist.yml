env: test

app_project: gosoline
app_family: test
app_name: rabbitmq-test

test:
  components:
    rabbitmq:
      default:

rabbitmq:
  default:
    endpoint: amqp://localhost:5672
    timeout: 20
    retry_count: 4

